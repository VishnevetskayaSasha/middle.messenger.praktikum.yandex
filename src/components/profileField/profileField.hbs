<div class="profile-field">
  <label
    class="profile-field__label"
    for="{{name}}"
  >
    {{label}}
  </label>

  <input
    class="profile-field__input"
    id="{{name}}"
    name="{{name}}"
    type="{{type}}"
    value="{{value}}"
    readonly
  />
</div>
