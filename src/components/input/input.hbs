<div class="input">
  <label class="input__label" for="{{name}}">{{label}}</label>

  <input required
    class="input__field"
    id="{{name}}"
    type="{{type}}"
    name="{{name}}"
    placeholder="{{label}}"
  />
</div>